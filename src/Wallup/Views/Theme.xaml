<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Light frosted glass, the way macOS vibrancy reads: the blur comes from DWM, and
         everything here is the sheen laid over it. Ink is dark because the frost is light. -->

    <SolidColorBrush x:Key="Ink" Color="#F2101418" />
    <SolidColorBrush x:Key="InkMuted" Color="#8C101418" />
    <SolidColorBrush x:Key="Accent" Color="#FF007AFF" />

    <!-- The bright rim that makes glass read as a physical edge rather than a flat panel.
         Brighter at the top, where light would catch it. -->
    <LinearGradientBrush x:Key="Rim" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#F2FFFFFF" Offset="0" />
        <GradientStop Color="#8CFFFFFF" Offset="0.4" />
        <GradientStop Color="#59FFFFFF" Offset="1" />
    </LinearGradientBrush>

    <!-- The white wash that makes the glass read as light.
         DWM acrylic takes its luminance from whatever is behind the window, so over a dark
         wallpaper the frost alone comes out near-black. macOS gets its brightness from a
         heavy white layer on top of the blur, not from the blur itself - so that is what
         this is. The gradient across it is the shine. -->
    <LinearGradientBrush x:Key="Sheen" StartPoint="0.05,0" EndPoint="0.8,1">
        <GradientStop Color="#C7FFFFFF" Offset="0" />
        <GradientStop Color="#ADFFFFFF" Offset="0.3" />
        <GradientStop Color="#9EFFFFFF" Offset="0.68" />
        <GradientStop Color="#AAFFFFFF" Offset="1" />
    </LinearGradientBrush>

    <!-- A specular band across the top few pixels, the way light catches a real edge. -->
    <LinearGradientBrush x:Key="TopLight" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#A6FFFFFF" Offset="0" />
        <GradientStop Color="#33FFFFFF" Offset="0.5" />
        <GradientStop Color="#00FFFFFF" Offset="1" />
    </LinearGradientBrush>

    <SolidColorBrush x:Key="Hairline" Color="#26101418" />

    <!-- Wraps a window's content. Sits on top of the DWM acrylic and supplies the rim,
         the sheen and the corner radius. -->
    <Style x:Key="GlassSurface" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Sheen}" />
        <Setter Property="BorderBrush" Value="{StaticResource Rim}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style x:Key="Caption" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource InkMuted}" />
        <Setter Property="FontSize" Value="11" />
    </Style>

    <!-- Text that lives directly on glass: no box, no border, no focus rectangle. -->
    <Style x:Key="GlassText" TargetType="TextBox">
        <Setter Property="Background" Value="{x:Null}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Foreground" Value="{StaticResource Ink}" />
        <Setter Property="CaretBrush" Value="{StaticResource Accent}" />
        <Setter Property="SelectionBrush" Value="{StaticResource Accent}" />
        <Setter Property="SelectionOpacity" Value="0.25" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <ScrollViewer x:Name="PART_ContentHost" Focusable="False"
                                  Background="{x:Null}" BorderThickness="0"
                                  HorizontalScrollBarVisibility="Hidden"
                                  VerticalScrollBarVisibility="Hidden" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Chip text at rest. A label, not a text box: a TextBox drags its own chrome onto
         the glass even with a transparent background, which shows as a dull panel behind
         every task. The editor only appears on a double-click. -->
    <Style x:Key="ChipLabel" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Ink}" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsDone}" Value="True">
                <Setter Property="Foreground" Value="#59101418" />
                <Setter Property="TextDecorations" Value="Strikethrough" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- A tick that reads as a task marker, not a Win32 control. -->
    <Style x:Key="TaskCheck" TargetType="CheckBox">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Border x:Name="Box" Width="17" Height="17" CornerRadius="9"
                            BorderBrush="#73101418" BorderThickness="1.3"
                            Background="#26FFFFFF" SnapsToDevicePixels="True">
                        <Path x:Name="Tick" Data="M 2.5,6.5 L 5.5,9.5 L 11,3.5"
                              Stroke="White" StrokeThickness="2"
                              StrokeEndLineCap="Round" StrokeStartLineCap="Round"
                              StrokeLineJoin="Round" Visibility="Collapsed" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource Accent}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Tick" Property="Visibility" Value="Visible" />
                            <Setter TargetName="Box" Property="Background" Value="{StaticResource Accent}" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource Accent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GhostButton" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource InkMuted}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="6,2" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Chrome" Background="{TemplateBinding Background}"
                            CornerRadius="6" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="#33FFFFFF" />
                            <Setter Property="Foreground" Value="{StaticResource Ink}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
