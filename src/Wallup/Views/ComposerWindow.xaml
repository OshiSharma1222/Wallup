<Window x:Class="Wallup.Views.ComposerWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wallup"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Width="310"
        Topmost="True"
        WindowStartupLocation="Manual">

    <Border Style="{StaticResource GlassSurface}">
        <Grid>
            <Border VerticalAlignment="Top" Height="18" CornerRadius="10,10,0,0"
                    Background="{StaticResource TopLight}" IsHitTestVisible="False" />

        <Grid Margin="16,13,16,13">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBox Grid.Row="0" x:Name="Input"
                     Style="{StaticResource GlassText}"
                     FontSize="16"
                     KeyDown="OnInputKeyDown" />

            <Rectangle Grid.Row="1" Height="1" Margin="0,9,0,0"
                       Fill="{StaticResource Hairline}" />

            <TextBlock Grid.Row="2" Style="{StaticResource Caption}" Margin="0,8,0,0"
                       Text="enter to drop it on the desktop  ·  esc to cancel" />
        </Grid>
        </Grid>
    </Border>
</Window>
